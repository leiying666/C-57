#include<stdio.h>

int main()
{
	int a, b, c;
	float sum = 0;
	scanf("%d%d%d", &a, &b, &c);
	for (int i = 1; i <= a; i++)
	{
		sum += i;
	}
	for (int j = 1; j <= b; j++)
	{
		sum += j*j;
	}
	for (int k = 1; k <= c; k++)
	{
		sum += 1.0 / k;
	}
	printf("%.2f\n", sum);
	return 0;
}
